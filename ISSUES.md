# ISSUES.md

This document references known issues for agents to pick them and iterate on

## List

- we don't have consistency across the map lines for routes or walkTo (point to point) tracing. we want a point to be blue in the map, when tracing a route, we want an orange straight line going from points to point, linking them together, the line that has already been walked through should be dashed. similar should be done for walkTo (point to point), 1 point at the start poisition, one at the target. make the UX consistent between the map screen and floating map view
